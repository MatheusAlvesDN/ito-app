1. **Add `aria-label="Voltar"` to `src/App.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

2. **Add `aria-label="Voltar"` to `src/components/lobby/ConnectionSelectionScreen.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

3. **Add `aria-label="Voltar"` to `src/components/lobby/LobbySetupScreen.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

4. **Add `aria-label="Voltar"` to `src/components/lobby/MultiplayerLobbyScreen.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

5. **Add `aria-label="Voltar"` to `src/impostor/RegisterScreen.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

6. **Add `aria-label="Voltar"` to `src/impostor/ThemeSelection.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

7. **Add `aria-label="Voltar"` to `src/impostor/GameScreen.tsx` navigation buttons**
   - Use `replace_with_git_merge_diff` to add the attribute to the multiple `<button onClick={onBack}>` containing `ChevronLeft`.

8. **Add `aria-label="Voltar"` to `src/ito/ThemeSelection.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`. Note: `src/ito/RegisterScreen.tsx` already has it!

9. **Add `aria-label="Voltar"` to `src/ito/GameScreen.tsx` navigation button**
   - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

10. **Add `aria-label="Voltar"` to `src/whoami/RegisterScreen.tsx` navigation button**
    - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

11. **Add `aria-label="Voltar"` to `src/whoami/ThemeSelection.tsx` navigation button**
    - Use `replace_with_git_merge_diff` to add the attribute to the `<button onClick={onBack}>` containing `ChevronLeft`.

12. **Add `aria-label="Voltar"` to `src/whoami/GameScreen.tsx` navigation buttons**
    - Use `replace_with_git_merge_diff` to add the attribute to the multiple `<button onClick={onBack}>` containing `ChevronLeft`.

13. **Verify build and types**
    - Run `pnpm build` and `pnpm lint` to ensure no errors were introduced.

14. **Pre-commit step**
    - Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
