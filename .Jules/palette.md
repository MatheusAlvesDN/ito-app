## 2025-02-28 - ARIA Labels for Icon-Only Buttons
**Learning:** React elements utilizing SVG icons inside buttons without explicit text need `aria-label` attributes to ensure screen readers convey the action accurately to users, matching the primary language (Portuguese).
**Action:** Always verify that icon-only buttons have descriptive `aria-label`s.
