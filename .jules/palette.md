## 2024-07-05 - Accessibility parity across similar screens
**Learning:** Component structures replicated across different game modes (ito, impostor, whoami) can easily fall out of sync with accessibility improvements (like missing ARIA labels on icon-only buttons).
**Action:** Always check sibling components in other game directories when fixing missing accessibility labels in one mode's screen.
