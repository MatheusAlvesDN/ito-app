## 2024-06-26 - Missing explicit label associations
**Learning:** Explicit `htmlFor` and `id` links on configuration inputs are crucial for proper accessibility on user-facing forms, ensuring screen readers properly associate the label with the input.
**Action:** Always associate `<label>` elements with their corresponding `<input>` elements using matching `htmlFor` and `id` attributes.
