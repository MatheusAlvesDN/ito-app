## 2024-05-18 - Added ARIA labels to icon-only buttons
**Learning:** Icon-only buttons without `aria-label` or accessible names are a common accessibility barrier in React applications.
**Action:** Always verify icon-only buttons (`<button><Icon/></button>`) have an explicit `aria-label` describing their function.
