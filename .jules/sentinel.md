## 2024-05-15 - [Infinite Loop DoS & State Memory Exhaustion in Game Initialization]
**Vulnerability:** The application allowed an unbounded number of players to be added to the state with arbitrarily long names. In , player numbers were uniquely generated via a  loop bounding the random choices to 100. Adding more than 100 players would result in an infinite loop, causing a Denial of Service (DoS) and browser freeze.
**Learning:** Client-side React state arrays populated by unbounded user inputs are vulnerable to memory exhaustion, and tightly bounded looping logic (e.g., uniqueness constraints on small integer pools) can easily be exploited to cause infinite loops if input size isn't validated.
**Prevention:** Always strictly enforce  on user text inputs and implement a hard limit on the number of items that can be added to dynamic client-side lists (e.g., maximum of 20 players), particularly when downstream logic depends on those list sizes being within specific bounds.
## 2024-05-15 - [Infinite Loop DoS & State Memory Exhaustion in Game Initialization]
**Vulnerability:** The application allowed an unbounded number of players to be added to the state with arbitrarily long names. In `GameScreen`, player numbers were uniquely generated via a `do...while` loop bounding the random choices to 100. Adding more than 100 players would result in an infinite loop, causing a Denial of Service (DoS) and browser freeze.
**Learning:** Client-side React state arrays populated by unbounded user inputs are vulnerable to memory exhaustion, and tightly bounded looping logic (e.g., uniqueness constraints on small integer pools) can easily be exploited to cause infinite loops if input size isn't validated.
**Prevention:** Always strictly enforce `maxLength` on user text inputs and implement a hard limit on the number of items that can be added to dynamic client-side lists (e.g., maximum of 20 players), particularly when downstream logic depends on those list sizes being within specific bounds.
